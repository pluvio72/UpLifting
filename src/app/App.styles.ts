import { StyleSheet } from "react-native";
import { colors } from "../util/styles";

export default StyleSheet.create({
  container: {
    flex: 1,
    height: '100%',
    width: '100%',
  },
  main: {
    flexGrow: 1,
  },
  navbar: {
    flexShrink: 1,
  }
})