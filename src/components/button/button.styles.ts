import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    borderRadius: 12,
    padding: 10,
    textAlign: 'center',
  },
  text: {}
});
export default styles;