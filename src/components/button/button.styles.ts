import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    borderRadius: 12,
    padding: 10,
    textAlign: 'center',
    flexDirection: 'row',
  },
  text: {},
  icon: {
    marginLeft: 6,
    marginTop: 2
  }
});
export default styles;